# frontend

Vite React SPA with TanStack Router and stock shadcn components on Base UI.

- use pnpm for package management
- keep `AppShell` mounted across route navigation so draft and stream state survive
- preserve same-origin production API, SSE, and WebSocket URLs
- run `pnpm test`, `pnpm lint`, and `pnpm build` after changes
