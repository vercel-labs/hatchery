# Public origin that DevBox can reach for task webhooks.
# Laptop: run `ngrok http 8000` and use its HTTPS origin.
# Sandbox: use the sandbox's existing forwarded public origin; do not run ngrok.
HATCHERY_PUBLIC_URL=https://your-public-origin.example
