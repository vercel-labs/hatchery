# Backend configuration is normally supplied by the deployment environment.
# Local development uses the same Vercel Sandbox and Queue workflow as cloud.
# Pull the required Vercel credentials into backend/.env.local before running dev.py.
# AI_GATEWAY_API_KEY=[redacted]
