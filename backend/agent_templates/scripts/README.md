# Scripts

Small executable helpers owned by this agent. Give each script a useful `--help`,
keep dependencies explicit, and never embed credentials. Scripts are stored and
shown in the agent file tree; they are not run during context loading.
