# Skills

A skill is a reusable procedure in `skills/<name>/SKILL.md` with standard YAML
frontmatter:

```markdown
---
name: release-checklist
description: Cut a release from changelog through verification.
---

# Release checklist
```

Names use lowercase hyphenated slugs. Keep names unique and descriptions clear
about when the skill applies. Put focused support files beside SKILL.md.
