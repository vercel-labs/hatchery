"""Git-backed agent directories and the shared wiki in the storage repository.

Ported from agentmesh `workspace/`. Import the modules directly (`repo`, `review`,
`browser`, `connect`, ...); nothing is re-exported here.
"""
